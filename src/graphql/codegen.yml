overwrite: true
schema:
  - ./src/graphql/appsync.graphql
  - ./src/graphql/schema.graphql

config:
  scalars:
    AWSJSON: string
    AWSDate: string
    AWSTime: string
    AWSDateTime: string
    AWSTimestamp: number
    AWSEmail: string
    AWSURL: string
    AWSPhone: string
    AWSIPAddress: string
generates:
  ./src/graphql/appsync.d.ts:
    plugins:
      - typescript